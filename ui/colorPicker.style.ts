import { css } from 'styled-components'

// noinspection CssUnresolvedCustomProperty
const colorPickerStyle = css`
  display: flex;
  box-sizing: border-box;

  background-color: var(--colorpicker-bc, transparent);
`

export default colorPickerStyle
